import { mount } from '@vue/test-utils'
import { createPinia, setActivePinia } from 'pinia'
import { describe, it, expect, beforeEach } from 'vitest'

describe('NodeTypeModal', () => {
  it('should be tested', () => {
    // 占位测试，避免测试失败
    expect(true).toBe(true)
  })
})